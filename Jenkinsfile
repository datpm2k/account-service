pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Starting the build Stage'
                echo 'Build Stage completed successfully'
            }
        }
    }
}